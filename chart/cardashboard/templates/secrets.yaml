---
apiVersion: v1
kind: Secret
metadata:
  name: binding-my-conversation-service
data:
  url: "{{ .Values.cloud_services.conversation.url | b64enc }}"
  username: "{{ .Values.cloud_services.conversation.username | b64enc }}"
  password: "{{ .Values.cloud_services.conversation.password | b64enc }}"
---
apiVersion: v1
kind: Secret
metadata:
  name: binding-speech-to-text-service
data:
  url: "{{ .Values.cloud_services.speech_to_text.url | b64enc }}"
  username: "{{ .Values.cloud_services.speech_to_text.username | b64enc }}"
  password: "{{ .Values.cloud_services.speech_to_text.password | b64enc }}"
---
apiVersion: v1
kind: Secret
metadata:
  name: binding-text-to-speech-service
data:
  url: "{{ .Values.cloud_services.text_to_speech.url | b64enc }}"
  username: "{{ .Values.cloud_services.text_to_speech.username | b64enc }}"
  password: "{{ .Values.cloud_services.text_to_speech.password | b64enc }}"
---
