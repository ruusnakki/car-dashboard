---
apiVersion: v1
kind: Secret
metadata:
  name: binding-my-conversation-service
data:
  url: "{{ .Values.cloud-services.conversation.url | b64enc }}"
  username: "{{ .Values.cloud-services.conversation.username | b64enc }}"
  password: "{{ .Values.cloud-services.conversation.password | b64enc }}"
---
apiVersion: v1
kind: Secret
metadata:
  name: binding-speech-to-text-service
data:
  url: "{{ .Values.cloud-services.speech-to-text.url | b64enc }}"
  username: "{{ .Values.cloud-services.speech-to-text.username | b64enc }}"
  password: "{{ .Values.cloud-services.speech-to-text.password | b64enc }}"
---
apiVersion: v1
kind: Secret
metadata:
  name: binding-text-to-speech-service
data:
  url: "{{ .Values.cloud-services.text-to-speech.url | b64enc }}"
  username: "{{ .Values.cloud-services.text-to-speech.username | b64enc }}"
  password: "{{ .Values.cloud-services.text-to-speech.password | b64enc }}"
---
